# AIチャットモード(/chat)に必要な環境変数
# ローカルでは .env にコピーして値を設定する(Netlifyでは環境変数として設定)
OPENAI_API_KEY=
# 1にするとOpenAIを呼ばずfixtureをストリーミング返却する(開発用・APIキー不要)
# fixture idを含むメッセージ(例:「unresolved」)で応答パターンを切り替えられる
CHAT_USE_MOCK=
